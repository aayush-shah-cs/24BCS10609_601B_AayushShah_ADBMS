CREATE TABLE EMPLOYEE (
    EMP_ID INT PRIMARY KEY,
    EMP_NAME VARCHAR(50),
    SALARY NUMERIC
);

INSERT INTO EMPLOYEE VALUES (101, 'Amit', 30000);
INSERT INTO EMPLOYEE VALUES (102, 'Riya', 40000);
INSERT INTO EMPLOYEE VALUES (103, 'Rahul', 0);
INSERT INTO EMPLOYEE VALUES (104, 'Neha', 50000);
INSERT INTO EMPLOYEE VALUES (105, 'Karan', 35000);

SELECT * FROM EMPLOYEE;

DO $$
DECLARE
    emp_cursor CURSOR FOR
        SELECT EMP_ID, SALARY FROM EMPLOYEE;
    V_EMP_ID EMPLOYEE.EMP_ID%TYPE;
    V_SALARY EMPLOYEE.SALARY%TYPE;
BEGIN
    OPEN emp_cursor;

    LOOP
        FETCH emp_cursor INTO V_EMP_ID, V_SALARY;
        EXIT WHEN NOT FOUND;

        IF V_SALARY = 0 THEN
            RAISE NOTICE 'Salary is zero. Increment not possible.';
        ELSE
            UPDATE EMPLOYEE
            SET SALARY = V_SALARY * 1.10
            WHERE EMP_ID = V_EMP_ID;
        END IF;
    END LOOP;

    CLOSE emp_cursor;
END $$;

SELECT * FROM EMPLOYEE;